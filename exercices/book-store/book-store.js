//@ts-check

/**
 * Determines the price of the basket
 *
 * @param {number[]} books the basket of books
 *
 * @returns {number | undefined} the price with the biggest discount
 */
export const cost = (books) => {
  let cost
  let done = false

  while (books.length > 2 && !done){
    if (books.includes(1, 2, 3, 4, 5))
  }

  return cost
};
